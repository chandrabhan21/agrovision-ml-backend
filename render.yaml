services:
  - type: web
    name: agrovision-ml-backend
    runtime: python
    pythonVersion: 3.10.13
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    plan: free
